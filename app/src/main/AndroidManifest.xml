<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Android 11+: без queries isRecognitionAvailable() и resolveActivity() для STT всегда false -->
    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
        <intent>
            <action android:name="android.speech.action.RECOGNIZE_SPEECH" />
        </intent>
    </queries>

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

    <application
        android:name=".VoiceMindApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VoiceMind">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.VoiceMind"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>

        <activity
            android:name=".ui.screens.AlarmActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:launchMode="singleTask"
            android:showOnLockScreen="true"
            android:taskAffinity=""
            android:theme="@style/Theme.VoiceMind.Alarm"
            android:turnScreenOn="true" />

        <receiver
            android:name=".data.scheduling.ReminderAlarmReceiver"
            android:exported="false" />

        <receiver
            android:name=".data.scheduling.ReminderActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.example.voicemind.ACTION_REMINDER_DONE" />
                <action android:name="com.example.voicemind.ACTION_REMINDER_SNOOZE" />
                <action android:name="com.example.voicemind.ACTION_REMINDER_CANCEL" />
                <action android:name="com.example.voicemind.ACTION_REMINDER_DISMISS" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".data.scheduling.BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".ui.widget.MicWidgetReceiver"
            android:exported="true"
            android:label="@string/widget_mic_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_mic_provider" />
        </receiver>

        <receiver
            android:name=".ui.widget.ReminderListWidgetProvider"
            android:exported="true"
            android:label="@string/widget_list_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_list_provider" />
        </receiver>

        <receiver
            android:name=".ui.widget.WidgetToggleReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.example.voicemind.ACTION_WIDGET_TOGGLE" />
            </intent-filter>
        </receiver>

        <service
            android:name=".ui.widget.VoiceMindTileService"
            android:exported="true"
            android:icon="@drawable/ic_mic"
            android:label="@string/tile_voice_label"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>
    </application>

</manifest>
