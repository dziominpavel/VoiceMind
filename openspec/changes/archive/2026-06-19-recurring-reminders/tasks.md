## 1. Room модель и миграция

- [x] 1.1 Добавить `recurrenceRule: String?` (и при необходимости `recurrenceInterval: Int`) в `Reminder`.
- [x] 1.2 Повысить версию БД на +1.
- [x] 1.3 Написать миграцию (не destructive, по `/db-migration` workflow).
- [x] 1.4 Тест миграции.

## 2. Вычисление следующего срабатывания

- [x] 2.1 Реализовать `nextOccurrence(rule: String, fromMillis: Long): Long` с поддержкой типов: DAILY, WEEKDAYS, WEEKENDS, WEEKLY(day), MONTHLY(dayOfMonth).
- [x] 2.2 Обработать пропуски (всегда ближайшее будущее).
- [x] 2.3 Юнит-тесты: ежедневный, по будням (граница пт–пн), по выходным, еженедельный, ежемесячный, пропуск + DST.

## 3. Пересчёт при срабатывании и reschedule

- [x] 3.1 В fire-путь (`ReminderActionReceiver`/alarm-путь) перед переводом в TRIGGERED проверять `recurrenceRule`.
- [x] 3.2 При повторении: вычислить следующий `fireAt`, перепланировать, оставить `PENDING`.
- [x] 3.3 `BootReceiver.rescheduleAll` корректно планирует повторяющиеся (ближайшее будущее).
- [x] 3.4 Обновить `ReminderRepository` для поддержки обновления правила и остановки повторения.

## 4. Парсинг повторений

- [x] 4.1 Расширить `ReminderParser`: распознавание фраз «каждый день», «ежедневно», «по будням», «по выходным», «каждый <день>», «каждое <число> число».
- [x] 4.2 Заполнить `recurrenceRule` в `ParseResult`.
- [x] 4.3 Юнит-тесты парсера на новые фразы.

## 5. UI

- [x] 5.1 Показывать индикатор повторения в списке, деталях и подтверждении.
- [x] 5.2 Кнопка «Остановить повторение» в деталях и при срабатывании.
- [x] 5.3 Строковые ресурсы для правил повторения.

## 6. Проверка

- [x] 6.1 `openspec validate --all`.
- [x] 6.2 Сборка `:app:assembleDebug` и `:app:testDebugUnitTest`.
- [ ] 6.3 Ручной чек: ежедневное и «по будням» — срабатывают и перепланируются корректно.
