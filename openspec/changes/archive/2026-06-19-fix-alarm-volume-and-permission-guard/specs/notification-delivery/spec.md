## ADDED Requirements

### Requirement: Громкость будильника задаётся в процентах
Громкость будильника MUST задаваться и храниться в процентах в диапазоне `0..100` — в том же масштабе, который использует `AlarmSoundPlayer` при вычислении целевой громкости потока `STREAM_ALARM`. Слайдер громкости в настройках MUST работать в этом же диапазоне `0..100`, чтобы максимум слайдера соответствовал максимальной громкости будильника.

#### Scenario: Максимум слайдера = максимальная громкость
- **WHEN** пользователь ставит слайдер громкости будильника на максимум
- **THEN** сохранённое значение громкости равно `100`
- **AND** при срабатывании ALARM `AlarmSoundPlayer` устанавливает громкость `STREAM_ALARM` в `getStreamMaxVolume(STREAM_ALARM)`

#### Scenario: Половина слайдера = половина громкости
- **WHEN** пользователь ставит слайдер громкости будильника примерно на середину (`50`)
- **THEN** сохранённое значение громкости равно `50`
- **AND** целевая громкость потока равна `getStreamMaxVolume(STREAM_ALARM) * 50 / 100`

#### Scenario: Миграция старого значения громкости
- **WHEN** в хранилище есть значение `alarm_volume` из старого масштаба (`<= 15`) и миграция громкости ещё не выполнялась
- **THEN** значение один раз пересчитывается в проценты как `round(value / 15 * 100)` и сохраняется
- **AND** выставляется флаг, предотвращающий повторную миграцию

### Requirement: Гард разрешений при сохранении напоминания
При сохранении напоминания без разрешения `POST_NOTIFICATIONS` система MUST явно и действенно сигнализировать пользователю об отсутствии разрешения и инициировать его запрос; система MUST NOT завершать сохранение как тихий успех без какого-либо сигнала. Сигналы об отсутствии разрешения на уведомления и об отсутствии точных будильников MUST быть независимыми и не перетирать друг друга. Сам ввод пользователя при этом MUST сохраняться (напоминание не теряется).

#### Scenario: Сохранение без разрешения на уведомления
- **WHEN** пользователь сохраняет напоминание, а разрешение `POST_NOTIFICATIONS` не выдано (API 33+)
- **THEN** напоминание сохраняется и не теряется
- **AND** показывается действенное предупреждение с возможностью выдать разрешение (запрос разрешения или переход в настройки)
- **AND** сохранение не отображается как тихий успех

#### Scenario: Сохранение без точных будильников
- **WHEN** пользователь сохраняет напоминание, а разрешение на точные будильники отсутствует
- **THEN** напоминание сохраняется и планируется через fallback `setAndAllowWhileIdle`
- **AND** показывается предупреждение о возможной неточности срабатывания

#### Scenario: Отсутствуют оба разрешения
- **WHEN** при сохранении отсутствуют и `POST_NOTIFICATIONS`, и точные будильники
- **THEN** оба сигнала доносятся до пользователя
- **AND** ни одно из предупреждений не перетирает другое
