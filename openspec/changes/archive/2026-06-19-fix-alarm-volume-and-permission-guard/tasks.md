## 1. Громкость будильника в процентах

- [x] 1.1 В `SettingsScreen.kt` (`AlarmVolumeSlider`) изменить `valueRange` на `0f..100f` и шаги на 5% (`steps = 19`); подпись/hint при необходимости обновить.
- [x] 1.2 Убедиться, что `SettingsRepository.setAlarmVolume` коэрцит в `0..100` (уже так) и что дефолт = `100`.
- [x] 1.3 Проверить, что `AlarmSoundPlayer.play(volumePercent)` корректно работает при значениях `1..100` (формула `maxVolume * percent / 100`) — изменений в трактовке не требуется.

## 2. Одноразовая миграция старого масштаба громкости

- [x] 2.1 В `SettingsRepository` добавить ключ-флаг `alarm_volume_migrated_v2` (boolean).
- [x] 2.2 Реализовать одноразовый пересчёт: если флаг не выставлен и хранимое `alarm_volume` присутствует и `<= 15`, записать `round(value / 15 * 100)`, затем выставить флаг.
- [x] 2.3 Значения `> 15` и отсутствующие (дефолт 100) не трогать; миграцию выполнять идемпотентно.

## 3. Гард разрешений при сохранении напоминания

- [x] 3.1 В `VoiceMindViewModel.saveReminder` разнести проверки `POST_NOTIFICATIONS` и точных будильников так, чтобы сообщения не перетирали друг друга (собрать оба сигнала).
- [x] 3.2 Добавить состояние/событие ViewModel (например, флаг «нужно запросить разрешение на уведомления»), которое UI может потребить.
- [x] 3.3 Сохранять напоминание (не терять ввод) даже при отсутствии разрешений; не отображать тихий успех при отсутствии `POST_NOTIFICATIONS`.
- [x] 3.4 В `MainActivity` подписаться на событие и запускать `notificationPermissionLauncher` (уже существует) и/или показывать действенный Snackbar с переходом в настройки.
- [x] 3.5 Для отсутствия точных будильников оставить fallback-планирование и показать предупреждение о возможной неточности.

## 4. Локализация и строки

- [x] 4.1 Добавить/проверить строковые ресурсы для предупреждений (уведомления отключены, неточный alarm) в `res/values/strings.xml`. (Существующие `error_notifications_permission`, `error_exact_alarm` покрывают случаи.)

## 5. Проверка

- [x] 5.1 `openspec validate --all` — без ошибок.
- [x] 5.2 Сборка `:app:assembleDebug` (или `:app:compileDebugKotlin`) — успешно. (`:app:testDebugUnitTest` — BUILD SUCCESSFUL)
- [x] 5.3 Юнит-тесты `:app:testDebugUnitTest` — зелёные.
- [ ] 5.4 Ручной чек: слайдер на максимум → будильник звучит на полную; создание напоминания без разрешения на уведомления → видно действенное предупреждение и запрос разрешения.
