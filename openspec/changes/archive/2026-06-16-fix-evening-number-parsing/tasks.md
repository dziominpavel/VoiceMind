## 1. Исправить TIME_4DIGIT

- [x] 1.1 Изменить regex `TIME_4DIGIT` в `ReminderParser.kt`: для 3-значных последовательностей требовать обязательное ведущее «в »; 4-значные оставить без изменений.
- [x] 1.2 Убедиться, что `TIME_4DIGIT` не матчит «100» в контексте «про 100 рублей».
- [x] 1.3 Убедиться, что «в 930» и «2130» всё ещё распознаются корректно.

## 2. Добавить тесты

- [x] 2.1 Добавить тест `tomorrowEvening_withNumberQuantity` — «завтра вечером напомнить про 100 рублей» должен давать `fireAt = завтра 22:00`, `body = «напомнить про 100 рублей»`.
- [x] 2.2 Добавить тест `threeDigitTime_withLeadingV` — «в 930 встреча» должен давать `fireAt = 09:30`.
- [x] 2.3 Добавить тест `fourDigitTime_noSeparator` — «встреча в 2130» должен давать `fireAt = 21:30`.
- [x] 2.4 Добавить тест `quantityGrams_withMorningMarker` — «купить 250 грамм завтра утром» должен давать `fireAt = завтра 09:00`, `body` содержит «250 грамм».
- [x] 2.5 Запустить существующие тесты `ReminderParserTest` — убедиться, что регрессии нет.

## 3. Валидация

- [x] 3.1 Запустить `openspec validate --all` и убедиться, что specs проходят валидацию.
- [x] 3.2 Запустить `./gradlew test` (или `gradlew test`) и убедиться, что все unit-тесты проходят.
