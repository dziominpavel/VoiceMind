## 1. Тесты (сначала test-case, потом реализация)

- [x] 1.1 Добавить тест `nominativeMarch_withTime` — «5 март в 10:00 дело» → 5 марта в 10:00, body «дело»
- [x] 1.2 Добавить тест `nominativeAugust_withYear` — «12 август 2026 в 9:00 праздник» → 12 августа 2026 в 09:00, body «праздник»
- [x] 1.3 Добавить regression-тесты на номинативы всех 12 месяцев (параметризованный или 12 отдельных @Test)
- [x] 1.4 Добавить тест `unrecognizedMonthName_notDecember` — «5 абракадабра в 10:00 дело» → дата не извлекается, body сохраняет текст
- [x] 1.5 Запустить тесты — убедиться, что новые тесты на «март»/«август»/«абракадабра» падают (red), а regression-тесты на «июнь»/«сентябрь»/genitive проходят

## 2. Реализация фикса

- [x] 2.1 Создать `MONTH_MAP: Map<String, Int>` в companion object с обоими склонениями (genitive + nominative) для всех 12 месяцев
- [x] 2.2 Заменить тело `monthFromName` на lookup в `MONTH_MAP` с возвратом `-1` для нераспознанных имён
- [x] 2.3 В блоке `DATE_DAY_MONTH` (строки 373-391) убрать `.let`-костыль, использовать `monthFromName` напрямую, при `-1` — `return@forEach`
- [x] 2.4 Удалить функцию `monthFromNominative` (стала мёртвым кодом)

## 3. Верификация

- [x] 3.1 Запустить все тесты `ReminderParserTest` — все зелёные
- [x] 3.2 Запустить `openspec validate --all` — без ошибок
- [x] 3.3 Проверить, что существующие тесты на genitive (например «25 мая») и номинативы на «ь»/«май» (например «25 июнь») не сломались
