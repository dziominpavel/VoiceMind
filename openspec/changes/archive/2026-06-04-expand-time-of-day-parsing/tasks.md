## 1. Парсер — маркеры времени суток

- [x] 1.1 Добавить data class `PartOfDayCandidate` с `kind: MORNING | DAY | EVENING | NIGHT` и маппингом канонических часов (09:00 / 13:00 / 22:00 / 01:00).
- [x] 1.2 Добавить regex'ы для маркеров времени суток (`утро/утра/утром`, `день/дня/днём`, `вечер/вечера/вечером`, `ночь/ночи/ночью`) в `ReminderParser`.
- [x] 1.3 Обновить `resolveBestTime` для слияния `PartOfDayCandidate` с примыкающими/пересекающимися `TimeCandidate`:
  - Голый час + маркер → заменить час на канонический.
  - Точное `HH:mm` + маркер → сохранить точное время (маркер только повышает confidence).
  - Автономный маркер → создать `TimeCandidate` с каноническим часом.
- [x] 1.4 Обновить исключение span'ов из body, чтобы удалять `PartOfDayCandidate`.
- [x] 1.5 Написать unit-тесты, покрывающие:
  - «в 10 вечера» → 22:00
  - «в 8 утра» → 09:00
  - «вечером» → 22:00
  - «18:30 вечера» → 18:30
  - «9 числа в 10 вечера сделать отчёт» → body «Сделать отчёт»

## 2. Парсер — порядковый день месяца

- [x] 2.1 Добавить regex `ORDINAL_DAY_PATTERN` для `\d{1,2}\s*(-го)?\s*числа`.
- [x] 2.2 Добавить создание `DateCandidate` для порядковых совпадений с `dayOfMonth` и выведенным месяцем (текущий или следующий, если день прошёл).
- [x] 2.3 Обновить исключение span'ов из body, чтобы удалять порядковые дни.
- [x] 2.4 Написать unit-тесты, покрывающие:
  - «15 числа» (сегодня 3 июня) → 15 июня
  - «9 числа» (сегодня 10 июня) → 9 июля
  - «3-го числа» → корректный парсинг с суффиксом
  - В сочетании с маркером времени: «9 числа в 10 вечера» → 9-е в 22:00

## 3. Документация и проверка

- [x] 3.1 Обновить `docs/REMINDER_PARSING.md` новыми поддерживаемыми паттернами (маркеры времени суток, порядковый день месяца).
- [x] 3.2 Запустить тестовый набор `ReminderParserTest` и убедиться, что все новые + существующие тесты проходят.
- [x] 3.3 Сделать быструю ручную проверку на 3–5 фразах, похожих на речь, чтобы убедиться, что body чистый.
